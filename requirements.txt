# Core dependencies to coastal application
folium==0.20.0
matplotlib==3.10.9
numpy==2.2.6
pandas==2.3.3
netCDF4==1.7.4
pyproj==3.7.1
ipywidgets==8.1.9
plotly==5.15
xarray==2025.6.1
PyYAML==6.0.3
anywidget==0.9.23
s3fs
pymupdf

# Optional: enables satellite/basemap tiles in a few plots (notebook falls
# back gracefully with a warning if this is not installed)
contextily==1.7.1

# Needed to run/open the notebook itself
ipykernel
nbformat
nbconvert

# Local `demonstrator` package (src/), installed in editable mode
-e .
